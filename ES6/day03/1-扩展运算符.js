//将字符串转成数组
let arr = [...'hello'];
console.log(arr)
//扩展运算符 后面如果是变量，那么这个变量将来是一个数组 存放的是多余的数据
// 扩展运算符 后面如果是数组，那么将这个数组展开 在参数那里使用
// 扩展运算符 后面如果是字符串，外部包裹[],就是一个数组
let arr2 = [...[1,2,3,4]]
console.log(arr2)